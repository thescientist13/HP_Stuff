---
import { Card } from '@astrojs/starlight/components';
let { id } = Astro.props;
id = `@${id}@`;
import { PersonBio } from './PersonBio';
const props = {url: Astro.url};
console.log(`BioCard props url is ${props.url}`)
---
<Card title="Biographical Information">
    <PersonBio gedid={id} url={Astro.url} />
</Card>