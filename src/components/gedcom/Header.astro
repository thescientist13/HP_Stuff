---
import { Card } from '@astrojs/starlight/components';
import { GedcomHeader } from './header';
let { file } = Astro.props;
const GedUrl = new URL(file, Astro.site );
console.log(`Gedcom url is ${GedUrl}`)
---
<Card title="Gedcom Header Information">
    <GedcomHeader file={GedUrl} client:only />
</Card>